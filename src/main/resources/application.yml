server:
  port: 8080
  error:
    include-message: always

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username : root
    url : jdbc:mysql://localhost:3306/tasktify_Db
    password: Qa123490
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: true

logging:
  level:
    root: INFO
    io.github.erwin.taskify_api: DEBUG
  file:
    name: logs/taskify-api.log